#include "definition.h"

void iterate_sgd(const smatrix *X,
                 const dvector *y,
                 dvector *w,
                 const double lambda,
                 const double stepsize);
